# A file based on this template (with the exact name "local_config") should be saved in the sc_magic superdirectory (the one containing the exp_wrapper.py script)

# Output directories are where all data and log files for the magic and non-magic versions of the experiment are saved at the end of each task
magic_output_directory=[insert absolute path]
mundane_output_directory=[insert absolute path]

# The test_directory is where output data files are saved for test runs of either game type (as opposed to real experiment runs)
test_directory=[insert absolute path]


