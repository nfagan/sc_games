# A file based on this template (with the exact name "local_config") should be saved in the stressor controllability superdirectory (the one containing the GameWrapper.py script)

# The output_directory is where output data files are saved at the end of each task (including all log files)
output_directory=[insert absolute path]

# The test_directory is where output data files are saved for test runs (as opposed to real experiment runs)
test_directory=[insert absolute path]

# The version of Python that has Psychopy installed
python_bin=[insert your version of Python that has Psychopy installed]

